// Q83
// this function change string to upparcase or lowercase
//this function change string to upparcase and lowercase
function convertstring(str) {
    var uppar = str.toUpperCase();
    var lowwer = str.toLowerCase();
    console.log("upparcase:", uppar);
    console.log("lowercase:", lowwer);
}
;
//change of case
convertstring("Hello sadaf");
